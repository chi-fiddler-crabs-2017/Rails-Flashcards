<h1> <%= @card.question %> </h1>

<%= form_for :guess, url: card_guesses_path(@card.id) do |f| %>
<p>
<%= f.label :answer %>
<%= f.text_field :guess_text %>
</p>
<p>
  <%= f.submit "Guess" %>
</p>

<% end %>
